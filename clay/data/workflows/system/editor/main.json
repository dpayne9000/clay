{
  "autoContext": "Create Clay workflows from the user's requirements and the supplied action contracts. Never select, imitate, or copy another workflow as a design strategy. Read an existing workflow only when the user explicitly asks to modify that exact workflow.",
  "workflow": {
    "steps": ["session"]
  },
  "actionSets": {
    "session": [
      {
        "id": "session_log",
        "type": "loop",
        "file": "./iteration.json",
        "iterations": 0,
        "continueKey": "keep_going",
        "outputKey": "turn_summary",
        "includedData": []
      }
    ]
  }
}
