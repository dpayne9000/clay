# 2026-08-12 — Complete CLI command reference

`docs/COMMANDS.md` now documents every active Clay command, subcommand,
argument, option, alias, and workflow-addressing form registered by
`clay/cli.py`. The documentation index links to the new reference, and release
and public-project inclusion lists ship it.
